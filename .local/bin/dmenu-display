#!/bin/bash
ANS=$(echo -e " Laptop\n TV" | rofi -dmenu -i -p 'Display')
if [ "$ANS" == " Laptop" ] ; then
	exec ~/.screenlayout/laptop.sh
elif [ "$ANS" == " TV" ] ; then
	exec ~/.screenlayout/tv.sh
else
	:
fi
